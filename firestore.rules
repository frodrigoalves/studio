
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read/write access to all collections for development.
    // This is NOT secure for production. You should implement proper security
    // rules before deploying your application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
